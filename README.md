Usage: `vsindexer [input_dir]`

Creates index files for all the vapoursynth scripts in a directory, recursively. Open sourced because it might be useful to someone else. This took like 5 minutes to write.
